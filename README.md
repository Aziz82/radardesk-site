# radardesk-site

The public pages for the RadarDesk iOS app: the support page and the privacy policy.

Plain HTML, one file per page, all CSS inline, no build step, no JavaScript, no fonts and no
third-party requests — a privacy policy that loaded a tracker would be an odd document.

Source of truth is the RadarDesk repository under `docs/`. Edit there, copy here, commit.
